# Exercícios: Funções e Organização do Código 🧩

!!! info "Instruções"
    Teste seus conhecimentos sobre funções, pacotes e ponteiros.

---

### 🟢 Nível: Básico

1.  **Sintaxe**: Crie uma função chamada `saudar` que recebe um nome (string) e retorna uma saudação formatada.
2.  **Múltiplos Retornos**: Escreva uma função que receba dois números e retorne a soma e a subtração deles simultaneamente.

---

### 🟡 Nível: Intermediário

3.  **Ponteiros**: Crie uma função `zerar` que recebe um ponteiro para um inteiro (`*int`) e define o valor no endereço de memória como 0.
4.  **Variádicas**: Escreva uma função `media` que aceite uma quantidade variável de números (`float64`) e retorne a média aritmética deles.

---

### 🔴 Nível: Desafio

5.  **Multi-Package**: Crie um pequeno sistema onde o pacote `matematica` contém uma função `Fatorial` e o arquivo `main.go` utiliza essa função para calcular o fatorial de um número digitado pelo usuário.
